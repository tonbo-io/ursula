node_modules
dist
.DS_Store
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Regenerated by `pagefind --site dist` during build, then copied to public/ so
# `npm run dev` can serve it. Ephemeral build output — not checked in.
public/pagefind/
